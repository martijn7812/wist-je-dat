import React from "react";
import ReactDOM from "react-dom/client";
import WistJeDatApp from "./wjdapp";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <WistJeDatApp />
  </React.StrictMode>
);